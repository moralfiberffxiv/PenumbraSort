name: Build and Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

env:
  DOTNET_VERSION: '10.0.101'

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download Dalamud
        shell: pwsh
        run: |
          Invoke-WebRequest -Uri "https://goatcorp.github.io/dalamud-distrib/net10/latest.zip" -OutFile latest.zip
          Expand-Archive -Force latest.zip "$env:AppData\XIVLauncher\addon\Hooks\dev"

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore dependencies
        shell: pwsh
        run: dotnet restore

      - name: Set version
        id: version
        shell: bash
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
            echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
            echo "IS_RELEASE=true"  >> $GITHUB_OUTPUT
          else
            echo "VERSION=1.0.0.0"  >> $GITHUB_OUTPUT
            echo "IS_RELEASE=false" >> $GITHUB_OUTPUT
          fi

      - name: Build
        shell: pwsh
        run: dotnet build --configuration Release /p:AssemblyVersion=${{ steps.version.outputs.VERSION }} /p:FileVersion=${{ steps.version.outputs.VERSION }}

      - name: Package plugin
        shell: pwsh
        run: |
          $outDir = "bin\x64\Release"
          if (!(Test-Path "$outDir\PenumbraSort.dll")) {
            Write-Error "DLL not found at $outDir\PenumbraSort.dll — searching for it..."
            Get-ChildItem -Recurse -Filter "PenumbraSort.dll" | Select-Object FullName
            exit 1
          }
          Compress-Archive -Path "$outDir\PenumbraSort.dll","$outDir\PenumbraSort.json" -DestinationPath "PenumbraSort.zip" -Force
          Write-Host "Packaged PenumbraSort.zip successfully."

      - name: Update repo.json
        if: steps.version.outputs.IS_RELEASE == 'true'
        shell: pwsh
        run: |
          $version = "${{ steps.version.outputs.VERSION }}"
          $content = Get-Content repo.json -Raw | ConvertFrom-Json
          $content[0].AssemblyVersion        = $version
          $content[0].TestingAssemblyVersion = $version
          $content[0].DownloadLinkInstall    = "https://github.com/${{ github.repository }}/releases/download/v${version}/PenumbraSort.zip"
          $content[0].DownloadLinkUpdate     = "https://github.com/${{ github.repository }}/releases/download/v${version}/PenumbraSort.zip"
          $content[0].DownloadLinkTesting    = "https://github.com/${{ github.repository }}/releases/download/v${version}/PenumbraSort.zip"
          $content | ConvertTo-Json -Depth 10 | Set-Content repo.json
          Write-Host "repo.json updated to version $version"

      - name: Commit updated repo.json
        if: steps.version.outputs.IS_RELEASE == 'true'
        shell: bash
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add repo.json
          git diff --staged --quiet || git commit -m "chore: bump repo.json to v${{ steps.version.outputs.VERSION }}"
          git push origin HEAD:main

      - name: Create GitHub Release
        if: steps.version.outputs.IS_RELEASE == 'true'
        uses: softprops/action-gh-release@v2
        with:
          files: PenumbraSort.zip
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: PenumbraSort-${{ steps.version.outputs.VERSION }}
          path: PenumbraSort.zip
          retention-days: 30
